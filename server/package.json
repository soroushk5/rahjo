{
  "name": "@rahjo/crm-bff",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Tenant-scoped Rahjo operational BFF for the separated Relaticle CRM service.",
  "engines": {
    "node": ">=22"
  },
  "scripts": {
    "start": "node src/index.js",
    "migrate": "node scripts/migrate.mjs",
    "provision": "node scripts/provision.mjs",
    "check": "node --check src/index.js && node --check src/app.js && node --check src/repository.js && node --check src/relaticleClient.js && node --check src/nativeDeferredCrmClient.js && node --check scripts/migrate.mjs && node --check scripts/provision.mjs && node --check scripts/restore-smoke.mjs",
    "test": "node --test tests/*.test.mjs"
  },
  "dependencies": {
    "postgres": "3.4.7"
  }
}
