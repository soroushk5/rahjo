import { siteShell } from "../../app/siteShell.js";
import { icon } from "../../components/icons.js";
const gates=["قرارداد منبع و حق پردازش","مشتری و کاربرد مجاز","رضایت و حداقل‌سازی داده","نگهداشت و حذف","امنیت و کنترل دسترسی","SLA و مالک رخداد"];
export function renderTrustPage(){const gateMarkup=gates.map((g,i)=>`<li><span>${String(i+1).padStart(2,"0")}</span><strong>${g}</strong><small>مدرک و مسئول مشخص</small></li>`).join("");const content=`<section class="page-hero trust-hero"><div class="container hero__grid"><div><p class="eyebrow">TRUST CENTER</p><h1>اعتماد، یک صفحه تبلیغاتی نیست.</h1><p>اعتماد در رهجو یعنی هر ادعا، منبع، مجوز، کنترل و مسئول پاسخ‌گو داشته باشد.</p></div><div class="trust-seal">${icon("shield",{size:52})}<strong>Evidence before claim</strong><small>مدرک پیش از ادعا</small></div></div></section><section class="container trust-layout"><div><p class="eyebrow">دروازه عرضه</p><h2>شش شرط پیش از فعال‌شدن هر سرویس واقعی</h2><p class="muted">تا وقتی یک سرویس این مسیر را کامل نکرده، در محصول فقط به‌صورت demo یا غیرفعال باقی می‌ماند.</p></div><ol class="gate-list">${gateMarkup}</ol></section><section class="container transparency-grid"><article><span>${icon("document")}</span><h3>ثبت منبع</h3><p>نام مالک، تاریخ و دامنه داده.</p></article><article><span>${icon("timeline")}</span><h3>رد تصمیم</h3><p>چه کسی، چه زمانی و چرا.</p></article><article><span>${icon("eye")}</span><h3>حداقل نمایش</h3><p>فقط داده لازم برای هدف مجاز.</p></article><article><span>${icon("shield")}</span><h3>توقف امن</h3><p>وقتی مدرک کافی نیست، سیستم نه می‌گوید.</p></article></section>`;return siteShell({content,activePath:"/trust"});}
