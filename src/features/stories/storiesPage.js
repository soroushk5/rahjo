import { siteShell } from "../../app/siteShell.js";
import { icon } from "../../components/icons.js";
import { editorialStories } from "../../data/siteContent.js";
export function renderStoriesPage(){const cards=editorialStories.map((s,i)=>`<article class="story-list-card"><div class="story-list-card__number">0${i+1}</div><div><div class="editorial-card__meta"><span>${s.section}</span><span>${s.time}</span></div><h2>${s.title}</h2><p>${s.dek}</p><span class="story-tag">${s.tag}</span></div><span class="story-list-card__arrow">${icon("arrow",{size:24})}</span></article>`).join("");const content=`<section class="page-hero page-hero--editorial"><div class="container split-heading"><div><p class="eyebrow">RAHJO STORY DESK</p><h1>دفتر روایت</h1></div><p>جایی برای پرونده‌ها، یادداشت‌های محصول و گزارش‌های ممیزی؛ چون اعتماد با توضیح ساخته می‌شود.</p></div></section><section class="container stories-list">${cards}</section><section class="container editorial-note"><span>${icon("story",{size:30})}</span><div><p class="eyebrow">سیاست تحریریه</p><h2>هر روایت باید مرز واقعیت، فرض و پیشنهاد را روشن کند.</h2><p>این اصل هم برای محتوا و هم برای رابط محصول اجرا می‌شود.</p></div></section>`;return siteShell({content,activePath:"/stories"});}
